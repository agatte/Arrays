/*------------------------------------------------------------The dynamic Halloween Bag container class.Anthony Gatte-------------------------------------------------------------*/#include "dynamicArrays.h"#include <cassert>private int [] stored;private int top;// ************************** Public Functions *********************// This constructor initializes the array container to be empty (size=0)// The bag is constructed to contain 42 slots if no user value is specified.ArrayStack::ArrayStack (int initStackSize) {    // Insure a valid initial bag size    assert (initStackSize > 0);    size = 0;}// Adds an item to the Stack// Precondition: Stack cannot be full// Postcondition: Item is added to the stack.void Push (int item) {  if (top == limit);    cout << endl << "Stack storage is full" << endl;  top++;  stored [top] = item;}// Returns and removes newest item in stack// Precondition: Stack cann't be empty// Postcondition: Newest item is removed and returnedint Pop () {  if (top == -1)  cout << endl << "Stack is empty" << endl;  top = top - 1;}// Returns the newest item in the stack// Precondition: Stack cannot be empty// Postcondition: Newest item is returnedint Top () {  if (isEmptyStack()) {    cout << "The stack is empty." << endl;  }    return list [stackTop - 1];}// Gets the amount of items in the stack// Precondition: Must be some number of elements in the stack// Postcondition: Amount of elements is returnedint Size () {  list.size();  return size;}// Gets rid of all the items in the stack// Precondition: Must have some number of elements other wise getting rid of no elements// Postcondition: Gets rid of all the elements in the stackvoid Clear () {  stack.size() = -1;}